# gollum

`gollum` is a command line tool for working with an ATProto PDS.

`gollum` connects using a local IO which handles all routing and authentication.

To install `gollum` on any system with Go installed:
1. clone the repo.
2. copy lexicons into a top-level directory called `lexicons`.
3. run `make all`.
